
def projectName = 'TH-E EMS Channel Manager - OpenMUC Application'

dependencies {
  compile project(':th-e-ems-core-api')

  compile fileTree(dir: 'dependencies/openmuc', include: ['*.jar'])
}

jar {
  manifest {
    name = projectName
    instruction 'Export-Package', ''
  }
}
