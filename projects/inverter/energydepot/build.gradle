repositories {
  flatDir {
    dirs rootDir.getPath() + "/dependencies/emonjava"
  }
}

dependencies {
  compile project(':th-e-core')
  compile project(':th-e-feature-emoncms')
}

jar {
  manifest {
    name = "TH-E Box Inverter - EnergyDepot"
    instruction 'Export-Package', ''
  }
}
