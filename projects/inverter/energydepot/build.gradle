configurations.create('embed')

def controlVersion = '0.1.3'

repositories {
  flatDir {
    dirs 'dependencies/TH-E-Control', rootDir.getPath() + "/dependencies/TH-E-Control"
  }
}

dependencies {
  compile project(':th-e-core')

  compile group: 'de.konstanz.isc', name: 'th-e-feature-emoncms', version: controlVersion
  embed group: 'de.konstanz.isc', name: 'th-e-feature-emoncms', version: controlVersion
}

jar {
  manifest {
    name = "TH-E Box Inverter - EnergyDepot"
    instruction 'Bundle-ClassPath', '.,lib/th-e-feature-emoncms-' + controlVersion + '.jar'
    instruction 'Import-Package', '!de.thebox.control.feature.emoncms*,*'
    instruction 'Export-Package', ''
  }
}

jar {
  into('lib') {
    from configurations.embed
  }
}
